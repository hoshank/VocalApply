# Dependencies
node_modules/

# Build output
dist/

# Environment. .env.example is the committed template; .env never is.
.env
.env*.local
!.env.example

# TypeScript incremental build state. Excluded because it embeds absolute
# paths from whichever machine ran the build.
*.tsbuildinfo

# OS files
.DS_Store
Thumbs.db
